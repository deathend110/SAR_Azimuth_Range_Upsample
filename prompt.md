好的，现在我们开始实际研究这个路线3的实验怎么做。
我全部的代码都放在Azimuth_Range_MixUpsample.m中。
目前的进度是我先把单独的距离向和方位向的上采样1bit量化过程搬过来了
目前我的问题是我应该关于上采样部分。
目前的单独方位向上采样和单独距离向上采样都是：
原始回波-上采样-RT阈值1bit量化-RC-下采样-RCMC-SAR成像
但是我现在要加入混合上采样的话，应该是怎么添加呢？
原始回波-距离上采样-方位向上采样-RT阈值1bit量化-RC-下采样-RCMC-SAR成像 or
原始回波-方位向上采样-距离上采样-RT阈值1bit量化-RC-下采样-RCMC-SAR成像

我做了这个As=0和As=0.6的实验，你看图：As0.6.jpg，As0.6.jpg。结果是说明2D的RT阈值也是有作用的，As=0下的结果不如As=0.6，随着As的提高，R2A2的指标表现差值更多。
先依据我上面的回答做分析，然后你这里的：
```text
再把 RT 统一成同一家族。最推荐的是给所有方法都用“可分离 2D RT”：
U = A_rt * exp(1i * (phi_r + phi_a))
```
是什么意思。先分别求出方位向上采样的RT阈值行向量和距离向上采样的RT阈值列向量，然后进行点混合？

你先初始化这个仓库，写入规则：
1. 我没明确告诉你写代码前不用写
2. 所有的代码输出都要附带合理的中文注释

GT是不受影响的。
下一步我这么设想的：
写一个脚本，对比我目前在Azimuth_Range_MixUpsample.m中的Build_2D_RT和Build_2D_SplitRT两种RT阈值生成方式分别在同一As参数下的R2A2的数值对比。对比方式是以GT不动，看看这两个RT阈值，你遍历整个SAR回波序列，分层抽样抽取20帧图出来，求同个As下的ssim和psnr均值，再进入下一个As重复上面的过程。As从0到1，步长0.2
你新建一个脚本，注意用中文注释

单个as抽取10张图就行，最后绘制两张图，一张图是psnr y轴，as x轴，另一张是ssim y轴，其他照上。图片布局美化什么的你自己设计，As步长0.1